# WorkFind 微信小程序方案文档

> 版本 v1.0 ｜ 2026-08-09 ｜ 状态：方案（待评审）
> 定位：workfind skill 的云端轻量版——**个人求职管理工具**（非招聘平台）

---

## 一、为什么做小程序

| 痛点 | 小程序解法 |
|------|-----------|
| skill 要装 Reasonix/电脑，门槛高 | 微信扫一扫/搜索即用，免安装 |
| 用户投递记录在本机，看不到全局 | openid 绑定 → **云端投递看板**，多设备同步 |
| 数据回收靠 webhook 攒数据 | 用户一键提交 → **直接进云数据库**，后台可导出 |
| 用户不知道"投了哪些" | 看板可视化：已投/笔试/面试/offer 状态卡片 + 统计 |

---

## 二、定位与资质红线（关键）

- **定位**：个人求职管理工具（记录投递、AI 求职建议、数据分析）
- **红线**：❌ 不做招聘信息发布、❌ 不做内推中介、❌ 不展示第三方招聘岗位列表
- 原因：个人主体小程序开通"招聘"类目需企业资质；工具类目个人可过审
- 单位信息查询 = 用户自己输入单位名，小程序给出公开信息分析（AI 生成建议），不算"招聘平台"

---

## 三、功能设计

### 阶段 1 MVP（最快上线，不依赖 AI）
1. **投递看板**
   - 添加投递：单位、岗位、地点、截止日期、状态（已投/笔试/面试/offer/拒/放弃）
   - 状态卡片流 + 顶部统计（总投递/进行中/offer 数）
   - 编辑/删除、按状态筛选、投递提醒（订阅消息）
2. **数据贡献**
   - 一键提交脱敏数据（学历/证书/关系/行业/地区/结果）→ 云数据库 `contributions`
   - 明示"不含个人信息"，可先预览再提交，可撤回
3. **我的**：简历摘要（手填几项）、隐私说明、关于

### 阶段 2（AI 能力，接大模型）
4. **单位查询**：输入单位名+岗位 → 云函数调 LLM + 搜索 → 返回匹配分析、录用参考区间（复用 skill 模型）
5. **录用参考**：论文基线模型可视化（基线→修正→区间）
6. **简历长短处**：按目标岗位出分析

### 阶段 3（完整）
7. 秋招日历、投递分析报表（offer 率、各环节转化）、模型校准提示

---

## 四、技术架构

```
微信小程序（原生 + TypeScript 可选）
├── pages/      投递看板、单位查询、数据贡献、我的
├── utils/      请求封装、脱敏工具
└── app.js      登录（openid）

微信云开发 CloudBase（免服务器）
├── 云数据库
│   ├── applications  投递记录（按 openid 隔离）
│   ├── profiles      用户画像（脱敏）
│   └── contributions 脱敏贡献数据（openid 匿名化后入库）
└── 云函数
    ├── login          获取 openid
    ├── apps CRUD      投递记录增删改查
    ├── contribute     数据贡献入库（自动脱敏）
    ├── aiQuery        阶段2：LLM 单位分析（接大模型 API，key 存环境变量）
    └── stats          阶段3：统计报表
```

### 数据模型（applications 集合）
```json
{
  "_id": "auto",
  "_openid": "用户openid",
  "company": "中国铁塔",
  "position": "综合管理",
  "location": "遂宁",
  "deadline": "2026-11-01",
  "status": "已投|笔试|面试|offer|拒|放弃",
  "result": null,
  "createdAt": 1234567890,
  "updatedAt": 1234567890
}
```

### 数据贡献脱敏（contribute 云函数内执行）
```json
{
  "学历": "硕士", "证书": "有", "关系": "有",
  "行业": "运营商", "岗位类型": "综合管理", "地区": "四川", "结果": "offer"
}
```
- 入库前剥离 `_openid`（只保留贡献记录，不关联用户）→ 管理员可导出，用户不可见他人

---

## 五、成本估算

| 项目 | 费用 |
|------|------|
| 微信云开发基础版 | **免费**（数据库 2GB、云函数调用额度、存储 5GB，个人可开通）|
| 小程序注册 | 免费（个人主体）|
| 阶段2 LLM API | 按量：DeepSeek/通义千问约 ¥0.001-0.002/千token，个人查询量成本可忽略 |
| 总计 | MVP 阶段接近 0 成本 |

---

## 六、部署步骤（你要做的，全程不写代码）

1. 注册小程序：mp.weixin.qq.com → 个人主体（身份证+手机号）→ 类目选"工具-效率"
2. 开通云开发：开发者工具 → 云开发 → 创建环境（免费基础版）
3. 拿代码：我把项目代码生成好后，微信开发者工具导入 → 上传
4. 提交审核：填"个人求职管理工具"，不涉及招聘中介
5. 发布后：分享小程序码，用户扫码即用

---

## 七、与 workfind skill 的关系

| | workfind skill | 微信小程序 |
|--|---------------|-----------|
| 用户 | 有电脑/Reasonix 的深度用户 | 所有微信用户（零门槛）|
| 投递记录 | 本地 md | 云端看板（多设备）|
| AI 查询 | subagent 全量跑 | 云函数按需调用 |
| 数据回收 | webhook（未配）| 云数据库直接入库 |
| 关系 | skill 是"模型引擎"，小程序是"产品外壳"——**模型/内参/反诈骗逻辑两者复用** |

**建议**：skill 继续维护（模型迭代的地方），小程序消费它的逻辑；小程序收集的真实数据反过来校准模型——数据飞轮完整闭环。

---

## 八、风险与注意

1. **审核**：强调"个人管理工具"，避免出现"招聘信息/找工作平台"字样的宣传图
2. **LLM 成本**：给 aiQuery 加频控（每用户每日次数），key 放云函数环境变量不暴露
3. **隐私**：贡献数据必须脱敏后入库；用户可删除自己的投递记录
4. **维护**：小程序上线后需长期维护（类目审核、版本迭代），确认有精力再启动

---

## 九、下一步（待确认）

- [ ] 确认方案 → 我生成小程序完整代码（页面+云函数+部署说明）
- [ ] 你注册小程序账号 + 开通云开发
- [ ] 导入代码 → 上传 → 审核 → 发布
